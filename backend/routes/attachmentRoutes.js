const express=require('express');const router=express.Router();const {upload,handleUploadError}=require('../middleware/uploadMiddleware');const {submitComplaintWithAttachment}=require('../controllers/attachmentComplaintController');const {protect,studentOnly}=require('../middleware/authMiddleware');router.post('/complaints/with-attachment',protect,studentOnly,upload.single('attachment'),handleUploadError,submitComplaintWithAttachment);module.exports=router;